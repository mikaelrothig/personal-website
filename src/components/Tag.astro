---
interface Props {
    label: string;
}

const { label } = Astro.props;
---

<div class="bg-primary-lighter px-3 py-1 rounded-full text-nowrap bg-teal-400/10">
    <p class="text-xs text-teal-300">{label}</p>
</div>
---
interface Props {
    label: string;
}

const { label } = Astro.props;
---

<div class="px-3 py-1 rounded-lg text-nowrap bg-accent-hover/10 backdrop-blur-md">
    <p class="text-xs text-accent">{label}</p>
</div>